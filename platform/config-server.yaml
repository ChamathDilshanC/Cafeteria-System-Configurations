spring:
  application:
    name: config-server

server:
  port: 9000

eureka:
  client:
    service-url:
      default-zone: http://localhost:9001/eureka/
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,env,refresh
