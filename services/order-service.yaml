spring:
  mvc:
    problemdetails:
      enabled: true
  datasource:
    url: jdbc:postgresql://localhost:5432/order_service_db
    username: postgres
    password: Chamath@123
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2
      idle-timeout: 300000
      max-lifetime: 600000
      connection-timeout: 30000
      validation-timeout: 5000
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    com.cafeteria.orderservice: DEBUG
    org.hibernate.SQL: DEBUG

server:
  port: 0

eureka:
  instance:
    instanceId: ${spring.application.name}:${random.value}
